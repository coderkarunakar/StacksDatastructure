#we can store elements by using array/list,LinkedList and another way of storing is using Stack
#we cannnot remove inbetween the stack 
#stack is about 3 things 
#1.Push(insert in stack)
#2.pop(delete in stack)
#3.top(Access the top element of stack)


#4.size(how many elements in stack)
#5.isEmpty(Tells is stack empty or not gives boolean value(True)or False)